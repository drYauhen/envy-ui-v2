{
  "$schema": "../../../../../schemas/dtcg-2025.10-schema.json",
  "eui": {
    "chart": {
      "legend": {
        "symbolSize": {
          "$value": "18",
          "$type": "string",
          "$description": "Visible legend symbol size in chart-like components (px, unitless for renderer adapters)."
        },
        "itemMinHeight": {
          "$value": "24",
          "$type": "string",
          "$description": "Minimum interactive legend item height (px, unitless for renderer adapters)."
        },
        "symbolPadding": {
          "$value": "8",
          "$type": "string",
          "$description": "Horizontal gap between legend symbol and label (px)."
        },
        "itemDistance": {
          "$value": "14",
          "$type": "string",
          "$description": "Horizontal distance between legend items (px)."
        },
        "padding": {
          "$value": "8",
          "$type": "string",
          "$description": "Internal legend container padding (px)."
        }
      }
    }
  }
}
