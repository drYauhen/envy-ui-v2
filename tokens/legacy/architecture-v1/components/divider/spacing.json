{
  "$schema": "./schemas/dtcg-2025.10-schema.json",
  "eui": {
    "divider": {
      "spacing": {
        "margin": {
          "vertical": {
            "$value": "0",
            "$type": "dimension",
            "$description": "No margin by default - divider relies on parent layout spacing (gap, padding, flex/grid). In inline contexts, add margin via wrapper padding or utility class."
          },
          "horizontal": {
            "$value": "0",
            "$type": "dimension",
            "$description": "No margin by default - divider relies on parent layout spacing (gap, padding, flex/grid). In inline contexts, add margin via wrapper padding or utility class."
          }
        }
      }
    }
  }
}





