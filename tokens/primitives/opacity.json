{
  "$schema": "../../schemas/dtcg-2025.10-schema.json",
  "eui": {
    "opacity": {
      "muted": {
        "$value": 0.6,
        "$type": "number",
        "$description": "Opacity for muted content blocks that should appear inactive but remain readable"
      },
      "disabled": {
        "$value": 0.5,
        "$type": "number",
        "$description": "Opacity for disabled elements"
      },
      "subtle": {
        "$value": 0.8,
        "$type": "number",
        "$description": "Opacity for subtle elements"
      }
    }
  }
}
