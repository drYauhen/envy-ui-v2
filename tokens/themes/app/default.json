{
  "eui": {
    "color": {
      "border": {
        "default": {
          "$value": "{eui.color.neutral.300}",
          "$type": "color",
          "$description": "Subtle border color for default theme in app context. More visible than neutral.200 but still minimal. Less prominent than accessibility theme."
        }
      }
    },
    "radius": {
      "none": {
        "$value": "0px",
        "$type": "dimension"
      },
      "small": {
        "$value": "2px",
        "$type": "dimension",
        "$description": "Smaller than default. Minimal theme keeps this subtle for compact layouts."
      },
      "default": {
        "$value": "4px",
        "$type": "dimension",
        "$description": "Baseline for minimal theme. All standard components use this value for consistent, stackable design."
      },
      "large": {
        "$value": "8px",
        "$type": "dimension",
        "$description": "Used for cards, panels, dropdowns. Provides visual hierarchy above control elements (buttons, inputs)."
      },
      "extra-large": {
        "$value": "12px",
        "$type": "dimension",
        "$description": "Used for modals, sheets, prominent containers. Maximum rounding for overlay elements."
      },
      "full": {
        "$value": "{eui.radius.pill}",
        "$type": "dimension"
      }
    }
  }
}

